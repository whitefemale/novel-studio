<!--
这个文件是 GitHub Release 的正文（.github/workflows/release.yml 里以 body_path 引用）。
发下一个版本时改这里、重新打 tag 即可。
-->
## ⚠️ 先读这一条：Windows 会拦你

安装包**没有做代码签名**，首次运行时 Windows 会弹出「Windows 已保护你的电脑」。

这是 SmartScreen 对「未知发布者」的默认拦截，**不是杀毒软件报毒，也不是安装包有问题**。
点「更多信息」→「仍要运行」即可正常安装。

不想看到这个提示的话，用下面的**便携版**：它不写注册表、不装任何东西，解压目录里双击 exe 就能跑。

---

## 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| Windows | `Novel-Studio-Setup-v1.0.0.exe` | 安装版（约 78 MB）。可选安装目录，建桌面和开始菜单快捷方式 |
| Windows | `Novel-Studio-Portable-v1.0.0.exe` | 便携版（约 78 MB）。单文件、双击即用、不写注册表，适合放 U 盘 |
| Android | `Novel-Studio-Android-*.apk` | 手机安装包（约 4 MB，debug 签名） |

浏览器版本不需要下载：克隆仓库后 `npm install && npm run dev` 打开 http://localhost:5173 即可。

## 首次使用

软件本身**不带 API Key**，第一次打开需要自己填一个大模型接口：

1. 右上角进「设置」；
2. 填 **Base URL**、**API Key**、**模型名**；
3. 默认 Base URL 是 `https://api.deepseek.com`，任何 OpenAI 兼容接口都能用（填到域名即可，`/chat/completions` 会自动补上）。

填完回书库新建一本书，就能用 AI 续写 / 生成大纲 / 生成章节了。

## 这个版本有什么

- **书库**：多本书卡片管理，新建 / 删除 / 编辑（书名、简介、封面配色），关键词搜索与排序
- **章节**：侧边栏目录，增删改与上下排序
- **编辑器**：纯文本 + Markdown 预览、实时字数统计、1.5 秒防抖自动保存（`Ctrl/Cmd+S` 立即保存）
- **查找替换**：`Ctrl/Cmd+F`，支持上一个 / 下一个 / 替换 / 全部替换
- **AI 续写**：从当前章节末尾自然衔接，自动带上大纲 / 人物 / 世界观与最近章节上下文
- **AI 扩写 / 改写**：选中文字后扩写或润色，一键替换或插入
- **生成大纲**：按书名与创意点生成完整分章大纲，存进设定
- **生成章节**：依据故事大纲生成整章正文，直接存为新章节
- **设定管理**：故事大纲 / 人物设定 / 世界观，自动注入 AI 提示词（多条大纲会全部注入）
- **导出**：整本或单章导出 TXT / Markdown；可设保存地址直写，也可每次弹保存框
- **自动备份**：按间隔把整本小说备份到 `保存地址/书名/备份/`，自动淘汰超出的旧备份

## 你的数据在哪

**全部在本机，不经过任何服务器。**

小说、章节、设定都存在本机 IndexedDB 里，API Key 也只存在本机。除了调用你自己在设置里填的那个
LLM 接口，软件**不会向任何地方发送数据**，也没有埋点、没有统计、没有账号体系。

导出的 TXT / Markdown 和自动备份是这套数据之外的另一份副本，两者互不影响。

## 已知限制

这些是环境与范围上的边界，不是待修的 bug：

- **未做代码签名**，安装版会被 SmartScreen 拦（见顶部说明）
- **Android 是 debug 签名的 APK**，仅供测试安装；未提供 release 签名版（需要自备 keystore）
- **不做 iOS**（无 macOS 构建环境）
- Android 版是同一套代码打包，但**窄屏布局、软键盘、返回键、分享导出只在真机上做过验证性构建**，未经完整真机回归

## 反馈

遇到问题欢迎提 [Issue](https://github.com/whitefemale/novel-studio/issues)，把操作系统、复现步骤和报错信息带上会更方便定位。
