# 统一换行符，避免 Windows 与 Linux/macOS 之间来回 checkout 产生整文件 diff。
# 仓库内存 LF，检出时按下面的规则还原。
* text=auto

# Gradle Wrapper 的 shell 脚本必须是 LF，否则在 Linux/macOS 上会报
# "/usr/bin/env: 'sh\r': No such file or directory"。
gradlew text eol=lf
*.sh text eol=lf

# Windows 批处理必须是 CRLF。
*.bat text eol=crlf

# 二进制文件不要做任何换行符转换。
# （* text=auto 本就会按 NUL 字节自动识别二进制，这里显式列出是为了意图明确）
*.jar binary
*.zip binary
*.apk binary
*.aab binary
*.ico binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.woff binary
*.woff2 binary
*.ttf binary

# 锁文件在 GitHub 的 PR 视图里折叠显示（不影响本地 git diff）
package-lock.json linguist-generated
